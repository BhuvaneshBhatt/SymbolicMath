# Notebooks

This directory contains runnable Jupyter notebooks for the `branchcuts` project.

- `demo_branchcuts.ipynb` — symbolic and numeric branch-cut examples

Run from the repository root after installing the package:

```bash
pip install -e .
jupyter lab
```
